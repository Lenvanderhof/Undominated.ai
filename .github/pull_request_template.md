<!-- Model rows, prices and scores are not edited here: file a Wrong figure issue with a primary source instead. -->

## What and why

## Checks

- [ ] `npm test` passes (Node ≥ 22.12)
- [ ] `node packages/undominated-check/prepublish.mjs` passes, if the CLI changed
- [ ] User-facing change noted in the package's `CHANGELOG.md`
- [ ] No hand-typed figure: anything numeric in the README comes from a generator
- [ ] Still quote-only and warn-never-fail; `unrated` is still not a score
